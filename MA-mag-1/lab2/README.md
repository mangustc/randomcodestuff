# Lab2

- NGINX container that routes traffic to other containers
- API container 1 with it's own NGINX that returns a simple string
- API container 2 with it's own NGINX that returns a different simple string


